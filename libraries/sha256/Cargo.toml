[package]
name = "sha256"
version = "0.1.0"
description = "SHA-256 and HMAC-SHA256 (via the hmac-sha256 crate) plus a MAC abstraction for the CellGuard firmware."
authors.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
hmac-sha256.workspace = true

[lints]
workspace = true
